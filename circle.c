#include<stdio.h>
int main(){
    float r,
    printf("Enter the radius of the circle :");
    scanf("%f",&r);
    float a=3.14*(r*r);
    printf("Area of the circle is : %f",a);
    return 0 ;
}